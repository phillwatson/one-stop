package com.hillayes.ftmarket.api.domain;

public enum CurrencyUnits {
    MAJOR, // currency values are specified in major units (e.g. pounds with pence fraction)
    MINOR; // currency values are specified in minor units (e.g. pence)
}
