quarkus:
  hibernate-orm:
    log:
      sql: true
      bind-parameters: true

  datasource:
    devservices:
      image-name: postgres:16.1-alpine3.19

one-stop:
  auth:
    xsrf:
      secret: this is a secret for xsrf tokens

  email:
    api-key: xxxxx
