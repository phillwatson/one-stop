quarkus:
  hibernate-orm:
    log:
      sql: true
      bind-parameters: true

  rest-client:
    nordigen-api:
      url: http://localhost:8089

  log:
    category:
      "org.apache.http.wire":
        level: DEBUG

one-stop:
  auth:
    xsrf:
      secret: "this is a secret for xsrf tokens"

  rail:
    callback-url: http://{{host-ip}}:9876/api/v1/rails/consents/response

  tests: # properties only used for testing
    nordigen:
      port: 8089

rails:
  secret:
    id: e9309c
    key: 8653cb
